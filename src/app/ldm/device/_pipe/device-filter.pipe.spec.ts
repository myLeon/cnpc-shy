import { DeviceFilterPipe } from './device-filter.pipe';

describe('DeviceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DeviceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
