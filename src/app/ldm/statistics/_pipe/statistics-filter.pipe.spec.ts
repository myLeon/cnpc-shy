import { StatisticsFilterPipe } from './statistics-filter.pipe';

describe('StatisticsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StatisticsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
