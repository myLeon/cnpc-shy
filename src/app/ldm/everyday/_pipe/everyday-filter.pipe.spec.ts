import { EverdayFilterPipe } from './everday-filter.pipe';

describe('EverdayFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EverdayFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
